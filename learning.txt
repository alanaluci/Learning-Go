errando ==> aprendendo ==> evoluindo


//operador de declaração ":=" "marmota",  só funciona dentro de um code block
// operador de atribuição "=", atribuição é global, declaração é especifico


quando a gnt faz a declaração da var sem dar o valor a gnt só pode colocar a atribuição dentro de um code block

Go não muda tipo de variavel, uma vez um tipo sempre o mesmo

entre acento ao contrário ` o tipo fica literal (rune literal)